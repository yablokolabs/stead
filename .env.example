# Stead Preview — example environment. PLACEHOLDERS ONLY.
#
# The real file lives at $STEAD_DEMO_HOME/.env (default ~/.stead-demo/.env),
# owner-only (mode 600), outside this repository. Never commit it.
# Never paste real values into a chat, a commit, or a systemd unit.

# --- Telegram ---------------------------------------------------------------
# From BotFather, for the "Stead Preview" bot ONLY.
# Must not be the token of any other bot on this machine.
STEAD_TELEGRAM_BOT_TOKEN=123456789:REPLACE_WITH_BOTFATHER_TOKEN

# Comma-separated numeric Telegram user IDs allowed to talk to Stead.
STEAD_ALLOWED_TELEGRAM_IDS=000000000

# One numeric chat ID that receives proactive reminders. This must be exactly
# one member of STEAD_ALLOWED_TELEGRAM_IDS; access may be multi-user, delivery
# must remain unambiguous.
STEAD_TELEGRAM_CHAT_ID=000000000

# --- Model: a Stead-owned APPLICATION API key -------------------------------
# This must be an application API key billed to the Stead demo.
# Do NOT use a personal Claude Code / ChatGPT / Gemini CLI subscription
# credential, and do not point Stead at another agent's OAuth tokens.
#
# provider: anthropic | gemini
STEAD_MODEL_PROVIDER=gemini

# Validated against the installed Hermes catalogue by scripts/check-secrets.sh,
# and re-checked against the profile config by scripts/stead-launch.sh on every
# start - a mismatch between the two fails closed.
# A model must clear BOTH gates or the gateway will not start:
#   1. present in this Hermes build's catalogue (models.py), and
#   2. available to your key — availability is per-key, not per-build.
# Verified on this build and this key:
#   gemini-3.1-flash-lite-preview   both       (current pin)
#   gemini-2.5-pro                  both       stronger, far lower free-tier headroom
#   claude-sonnet-4-6               catalogue  (needs an Anthropic key)
#   claude-opus-4-8                 catalogue  (needs an Anthropic key)
#   gemini-2.5-flash                KEY ONLY — not in the catalogue, will fail closed
# Check availability before pinning:
#   curl -s "https://generativelanguage.googleapis.com/v1beta/models?key=$GEMINI_API_KEY"
STEAD_MODEL_NAME=gemini-3.1-flash-lite-preview

# Supply ONLY the credential matching STEAD_MODEL_PROVIDER.
#   gemini    -> https://aistudio.google.com/app/apikey  (free tier available)
GEMINI_API_KEY=REPLACE_ME
#   anthropic -> https://console.anthropic.com/settings/keys  (requires credit)
# ANTHROPIC_API_KEY=sk-ant-REPLACE_ME

# --- Web search --------------------------------------------------------------
# A SearXNG instance on this VM. Not a secret, but security-relevant: whatever
# this points at receives every query Stead runs. The launcher scrubs any
# ambient SEARXNG_URL before sourcing this file, so it can only be set here.
#
# Set up with scripts/setup-searxng.sh, which writes the config and prints the
# docker command. Must stay on loopback — verify.sh fails if it is not.
#
# Leave this unset and Stead has no web tools at all: Hermes drops web_search
# and web_extract from the registry when no backend resolves.
# SEARXNG_URL=http://127.0.0.1:8080

# --- Locale ------------------------------------------------------------------
STEAD_TIMEZONE=Europe/London

# --- Workspace ---------------------------------------------------------------
# Holds the SQLite database, this .env, and logs. Outside git, mode 700.
STEAD_DEMO_HOME=/home/azureuser/.stead-demo
